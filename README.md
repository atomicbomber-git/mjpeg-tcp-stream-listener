# Simple Video Live Stream Server

## Setup

```shell script
# Install virtualenv if you haven't
sudo apt-get install python3-virtualenv    

# Initialize environment
python3 -m virtualenv -p python3 venv
```